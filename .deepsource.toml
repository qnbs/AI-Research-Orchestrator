version = 1

[[analyzers]]
name = "javascript"

  [analyzers.meta]
  plugins = ["react"]
  # Treat sources as ES modules so `import`/`export` parse cleanly.
  module = "esm"
  exclude_patterns = [
    "eslint-local-rules/**",
    "scripts/check-docs-drift.mjs",
  ]
