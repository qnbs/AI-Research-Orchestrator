# Consciousness Stream

Kurze, datierte Architektur- und Produktnotizen für Cursor-Kontext.

## 2026-05-04

- Cursor-Regeln konsolidiert: Always-On `.cursor/index.mdc`, modulare `.cursor/rules/*.mdc` inkl. Dexie (`101-dexie-local.mdc`), Meta (`000`), MCP/PRD (`850`). CI Typecheck = `npm run typecheck`. Repo-Docs und AUDIT an gleichen Stand gebracht.
