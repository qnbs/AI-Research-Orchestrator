# ==============================================================================
# Environment Variables for AI Research Orchestrator
# ==============================================================================
#
# IMPORTANT: This file is for DOCUMENTATION ONLY.
# The application uses a secure client-side API key input system.
# API keys are stored encrypted in the browser's IndexedDB.
#
# DO NOT commit actual API keys to version control!
#
# ==============================================================================

# [DEPRECATED] - API key is now entered via the UI Settings page
# The app no longer uses environment variables for API keys.
# This is much more secure for client-side applications.

# Node environment (automatically set by Vite)
# NODE_ENV=development
