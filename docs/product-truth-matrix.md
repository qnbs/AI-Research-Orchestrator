# Product truth matrix

Living map of user-visible claims vs runtime behavior. Update when onboarding, Help, chrome, or `index.html` copy changes. Gate: `pnpm run check:docs-drift` (`forbiddenProductCopyPhrases`). Agent merge policy is separate: `docs/pr-merge-gate.md`.

| Surface                                    | Claim the UI may make                                                                                                                                                                                   | Runtime truth                                                                                                                                                               | Must not claim                                                                  |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| Onboarding                                 | Client-only PWA for biomedical literature reviews. Heuristic runs the same phases without a key. Live mode sends prompts and metadata to the chosen provider and queries PubMed and, if enabled, arXiv. | Correct. Keys stay in the AES-GCM vault; encryption-at-rest is not XSS protection.                                                                                          | Swarm of agents; nothing is sent to a server; Future of Research.               |
| Literature review (`orchestrator`)         | Full pipeline: topic → PubMed/optional arXiv → rank → cited synthesis.                                                                                                                                  | Correct. Live ranks are the provider’s 1–100 estimate (`relevanceScale` omitted). Heuristic ranks are a 0–100 **display/relative** min-max scale (BM25+), not a live model. | Every sentence is PMID-grounded; heuristic equals Gemini.                       |
| Quick research (`research`)                | Rapid assistant: TL;DR / similar / one-question look.                                                                                                                                                   | Correct. Not the full review pipeline.                                                                                                                                      | That this tab is the product’s primary literature review.                       |
| Educational demo                           | Synthetic fixtures; labeled; existing demo banner.                                                                                                                                                      | Correct (ADR 0016). Never a silent stand-in for live retrieval.                                                                                                             | Demo hits are live PubMed.                                                      |
| Partial reports                            | Cancelled or restored runs are incomplete.                                                                                                                                                              | `'partial'`, never `'done'` (ADR 0021). Watermarks stay.                                                                                                                    | Cancelled runs are finished.                                                    |
| Inference badge / provider line            | Live vs heuristic vs forced vs offline. Ollama loopback: local generation; approved remote Ollama: generation goes to that endpoint. PubMed/arXiv still use the network when online.                    | Derived from key, network, Force-Heuristic, provider. Ollama status ignores leftover cloud default model names. Loopback vs remote copy follows `customBaseUrl`.            | The app is broken without a key. Every Ollama endpoint keeps prompts on-device. |
| Local AI (Ollama) Settings                 | Loopback health, discovered models, CORS vs timeout vs unavailable teaching, prompt-input budget, PubMed still uses the network.                                                                        | ADR 0019 first-classes loopback. Remote LAN needs tailored CSP. Ranking/synthesis omit articles that do not fit; empty fit is an error, not silent truncation.              | Remote LAN Ollama is first-class. Nothing ever leaves the device.               |
| Knowledge Base / Dashboard / History empty | Available after a saved review; CTA starts Literature review.                                                                                                                                           | Destinations stay reachable; empty states teach.                                                                                                                            | Dead disabled voids with no explanation.                                        |
| GitHub topics                              | Biomedical literature-review PWA (PubMed, KB, scientometrics, TypeScript).                                                                                                                              | Canonical names: `docs/project-facts.json` `githubTopics`. Sequential pipeline, not a multi-agent graph.                                                                    | `multi-agent-systems` as a product claim.                                       |
